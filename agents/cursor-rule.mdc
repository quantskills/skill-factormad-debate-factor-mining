---
description: 当用户请求使用 FactorMAD 风格的 LLM 多智能体辩论流程挖掘代码型股票 Alpha 因子时，使用本 Skill。
alwaysApply: false
---

# FactorMAD Debate Factor Mining Skill

当用户请求 FactorMAD、LLM 多智能体辩论、Alpha 因子挖掘、代码型因子生成、因子调试、因子验证或候选因子导出时，使用本地 Skill 包。

首选本地项目路径：

```text
.cursor/skills/factormad-debate-factor-mining
```

工作流：

1. 先读取 `.cursor/skills/factormad-debate-factor-mining/SKILL.md`。
2. 仅当 `SKILL.md` 要求或任务需要细节时，读取 `references/` 下的引用文件。
3. 按照 `SKILL.md` 的说明，从 Skill 根目录运行内置脚本。
4. 保持文档中定义的 API 名称、参数名、环境变量、文件路径、输出约定、验证边界和数据来源边界。
5. 不要编造 Skill 文件中未支持的数据接口、API key、评价指标、因子定义、输出字段或运行时行为。
6. 将输出视为量化研究候选因子，不要解释为投资建议、交易信号、收益承诺或生产交易验证。

## 运行入口

在仓库根目录运行：

```bash
python scripts/factormad_debate_factor.py --input examples/debate_input.json
```

使用自定义输入文件：

```bash
python scripts/factormad_debate_factor.py --input <input-json>
```

指定输出目录：

```bash
python scripts/factormad_debate_factor.py --input <input-json> --output <output-dir>
```

## 输入配置

运行前先阅读：

```text
references/input_schema.md
```

输入 JSON 至少需要提供：

```text
market_data_csv_path
```

真实 LLM 运行时应设置：

```json
"dry_run": false
```

测试安装、路径和输出格式时可以使用：

```json
"dry_run": true
```

## 环境变量

真实 LLM 运行需要配置 OpenAI 或 OpenAI-compatible 服务。

优先使用 FactorMAD 专用变量：

```bash
export FACTORMAD_OPENAI_API_KEY=...
export FACTORMAD_OPENAI_MODEL=...
export FACTORMAD_OPENAI_BASE_URL=...
```

也支持通用变量：

```bash
export OPENAI_API_KEY=...
export OPENAI_MODEL=...
export OPENAI_BASE_URL=...
```

仓库根目录 `.env` 也会被自动读取。

## 输出文件

运行后阅读：

```text
references/output_contract.md
```

稳定输出包括：

```text
factormad_debate_result.json
debate_rounds.json
accepted_factors.json
```

## 边界说明

本 Skill 用于量化研究自动化。IC/ICIR 只作为轻量候选筛选指标，不代表组合级回测结果，不构成投资建议、收益承诺或生产交易验证。
